export default function CommentSection() { // NOTE To do this first you'd need to implement github authentication with permission to write comment on gists as that is how the user is going to be authenticated in our system
    return (
        <section>
            <h2 className="text-xl cursor-default">Comments</h2>
        </section>
    )
}